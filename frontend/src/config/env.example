# Environment Variables Example
# Copy this to .env and modify as needed

# Backend API URL - corresponds to your existing BACKEND_URL
# NOTE: In Vite, variables must be prefixed with VITE_ to be accessible in frontend
VITE_BACKEND_URL=http://localhost:8000

# If you have existing BACKEND_URL in your .env, either:
# 1. Rename it to VITE_BACKEND_URL, or
# 2. Add this line: VITE_BACKEND_URL=${BACKEND_URL}

# For production, change to your actual backend URL
# VITE_BACKEND_URL=https://your-api-domain.com

# App information (optional)
VITE_APP_NAME=JobSeeker
VITE_APP_VERSION=1.0.0 